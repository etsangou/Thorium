#pragma once

#include "httplib.h"
#include <iostream>
#include <string>

class network_request {
public:
    static int get(std::string url)
    network_request(/* args */);
    ~network_request();
};
