[package]

name = "thorium-relay"
version = "0.1.0"
edition = "2021"

[dependencies]

quinn = "0.10"
tokio = { version = "1", features = ["full"]}
rustls = { version = "0.21", features = ["dangerous_configuration"]}
rcgen = "0.11"
prost = "0.12"

[build-dependencies]

prost-build = "0.12"
